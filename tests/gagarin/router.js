describe('Router tests', function () {

  var server = meteor();
  var client = browser(server);

  /*it('should change the route and load correct template', function () {
    return client
      .waitForRoute('/dropfile')
      .waitForDOM('#currentPageTitle')
      .getText('#currentPageTitle')
      .then(function(res) {
        expect(res).to.contain('Dropfile');
      });
  });

  it('should be ok returning to previous route', function () {
    return client
      .waitForRoute('/')
      .waitForDOM('#getText')
  });*/



});
